#ifndef SUPPORT_FUNC_H
#define SUPPORT_FUNC_H

#include <stddef.h>

short int bival_length (char *);
void swap_val (void *, void *, size_t);
void init_alph (char *);
int find_into_alph (char *, char);
void offset_alph (char *, int);

#endif
